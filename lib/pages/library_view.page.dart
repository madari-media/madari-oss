import 'package:flutter/material.dart';

class LibraryViewPage extends StatelessWidget {
  const LibraryViewPage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
